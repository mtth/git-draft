name: CI
on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yaml
  tag:
    name: Tag
    needs: test
    permissions:
      contents: write
    uses: ./.github/workflows/tag.yaml
  # TODO: Publish to Pypi.
